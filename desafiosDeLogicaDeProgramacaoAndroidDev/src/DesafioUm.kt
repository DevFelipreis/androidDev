fun main() {
    val a = 12
    val b = 32
    val c = 55
    val soma= a+b

    if(soma<c){
        println("A soma de A e B($soma) é menor que C($c)")
    }else{
        println("A soma de A e B($soma) não é menor que C($c)")
    }
}